specVersion: "v1beta"
serviceId: "soundwaves-service"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "soundwaves"
      cloudSql:
        instanceId: "soundwaves-db"
connectorDirs: ["./connector"]
